class Contato {
  nome: string
  apelido: string
  email: string
  tel: number

  constructor(nome: string, apelido: string, email: string, tel: number) {
    this.nome = nome
    this.apelido = apelido
    this.email = email
    this.tel = tel
  }
}

export default Contato
